## -----------------------------------------------------------------------------
library(BA22022002)

xs <- runif(1000)
ys <- 0.2*xs + runif(1000)
pearson.r(xs, ys)

## -----------------------------------------------------------------------------
rankdata(c(1, 5, 2))

## -----------------------------------------------------------------------------
xs[1] <- -100
ys[1] <- 100
c(pearson.r(xs, ys), spearman.r(xs, ys))

